import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionicVue3AppFreelance',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
